{
  "name": "json-server-api",
  "version": "1.0.0",
  "scripts": {
   "start": "PORT=${PORT:-3000} json-server db.json --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}